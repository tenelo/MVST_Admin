import 'package:flutter/material.dart';

class VerifParId extends StatelessWidget {
  const VerifParId({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
